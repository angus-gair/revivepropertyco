---
phase: quick
plan: 260429-4ur
type: execute
wave: 1
depends_on: []
files_modified:
  - /opt/homelab/apps/revivepropertyco/Dockerfile
autonomous: true
requirements: [SPA-ROUTING-FIX]
must_haves:
  truths:
    - "Deep links like /contractors/hp-leads return 200 with SPA content, not 301 redirect to root"
    - "Existing routes (/, /booking, /admin) continue to work"
  artifacts:
    - path: "Dockerfile"
      provides: "Correct nginx try_files directive without bare / fallback"
      contains: "try_files $uri $uri/ /index.html"
  key_links:
    - from: "Dockerfile nginx config"
      to: "SPA index.html"
      via: "try_files fallback"
      pattern: "try_files.*uri.*index\\.html"
---

<objective>
Fix SPA deep link routing by rebuilding the revivepropertyco container with the corrected Dockerfile.

Purpose: The running nginx container has a stale config with `try_files $uri $uri/ / /index.html` (extra bare `/` causing 301 redirects to root for all SPA routes). The Dockerfile on disk already contains the fix but was never committed or deployed.

Output: Working production deployment where deep links resolve correctly.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
The Dockerfile at `/opt/homelab/apps/revivepropertyco/Dockerfile` already has the correct
`try_files $uri $uri/ /index.html;` on disk (unstaged change). The running container still
has the broken directive. The main repo is on branch `feature/booking-redesign-v2`.

Key facts:
- Main repo path: `/opt/homelab/apps/revivepropertyco`
- Current worktree: `/opt/homelab/apps/revivepropertyco/.worktrees/feature-revive-links`
- The Dockerfile fix is in the MAIN repo (unstaged), not the worktree
- Docker compose file is at `/opt/homelab/apps/revivepropertyco/docker-compose.yml`
- Container name: `revivepropertyco`
- Live domain: `revivepropertyco.au`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit Dockerfile fix and rebuild container</name>
  <files>/opt/homelab/apps/revivepropertyco/Dockerfile</files>
  <action>
    1. In the main repo at `/opt/homelab/apps/revivepropertyco`, stage and commit the Dockerfile:
       ```bash
       cd /opt/homelab/apps/revivepropertyco
       git add Dockerfile
       git commit -m "fix: correct nginx try_files directive and disable caching for SPA routing"
       ```
    2. Rebuild and redeploy the app container:
       ```bash
       cd /opt/homelab/apps/revivepropertyco
       docker build -t revivepropertyco-app . && docker compose up -d app
       ```
    3. Wait for the container to become healthy (up to 60s).
  </action>
  <verify>
    <automated>docker exec revivepropertyco cat /etc/nginx/conf.d/default.conf | grep -c "try_files \$uri \$uri/ /index.html;" && echo "PASS: correct try_files" || echo "FAIL: wrong try_files"</automated>
  </verify>
  <done>Running container has correct try_files directive without the bare / fallback</done>
</task>

<task type="auto">
  <name>Task 2: Verify SPA deep links resolve correctly</name>
  <files></files>
  <action>
    Test that SPA deep link routes no longer redirect to root:
    1. `curl -sI https://revivepropertyco.au/contractors/hp-leads` -- should return 200, NOT 301
    2. `curl -sI https://revivepropertyco.au/booking` -- should return 200
    3. `curl -sI https://revivepropertyco.au/` -- should return 200 (sanity check root still works)

    If any return 301 redirecting to `/`, the fix did not take effect.
  </action>
  <verify>
    <automated>curl -so /dev/null -w "%{http_code}" https://revivepropertyco.au/contractors/hp-leads | grep -q "200" && echo "PASS" || echo "FAIL: got non-200 for deep link"</automated>
  </verify>
  <done>Deep link /contractors/hp-leads returns HTTP 200. No 301 redirect to root.</done>
</task>

</tasks>

<verification>
- `docker exec revivepropertyco cat /etc/nginx/conf.d/default.conf` shows `try_files $uri $uri/ /index.html;` (no bare `/`)
- `curl -sI https://revivepropertyco.au/contractors/hp-leads` returns 200
- `curl -sI https://revivepropertyco.au/booking` returns 200
- `curl -sI https://revivepropertyco.au/` returns 200
</verification>

<success_criteria>
All SPA deep link routes on revivepropertyco.au serve index.html (HTTP 200) instead of redirecting to root (HTTP 301).
</success_criteria>

<output>
No SUMMARY file needed for quick plans.
</output>
